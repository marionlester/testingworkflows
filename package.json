{
  "private": true,
  "version": "0.0.0",
  "name": "example",
  "devDependencies": {
    "@babel/core": "*",
    "@babel/preset-env": "*",
    "babel-jest": "*",
    "jest": "*"  
},
    "tests": [
    {
      "name": "jdom",
      "setup": "npm install --save jdom"
    }
  ],
  
  "private": true,
  "version": "3.2.5",
  "name": "jdom",
  "Dependencies": {
    "jdom": "*"
  }
,
    
  "scripts": {
    "test": "jest"
  }
}
